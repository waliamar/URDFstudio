[package]
name = "urdf-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
quick-xml = "0.36"
thiserror = "2"
ts-rs = { version = "10", features = ["serde-compat"] }
